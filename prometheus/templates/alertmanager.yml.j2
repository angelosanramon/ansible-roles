{%- if alertmanager_global_config|default({}, true)|length > 0 -%}
global:
  {{ alertmanager_global_config|to_nice_yaml(indent=2)|indent(width=2) }}
{% endif %}

{%- if alertmanager_templates_config|default([], true)|length > 0 -%}
templates:
  {{ alertmanager_templates_config|to_nice_yaml(indent=2)|indent(width=2) }}
{% endif %}

{%- if alertmanager_route_config|default({}, true)|length > 0 -%}
route:
  {{ alertmanager_route_config|to_nice_yaml(indent=2)|indent(width=2) }}
{% endif %}

{%- if alertmanager_receivers_config|default([], true)|length > 0 -%}
receivers:
  {{ alertmanager_receivers_config|to_nice_yaml(indent=2)|indent(width=2) }}
{% endif %}

{%- if alertmanager_inhibit_rules_config|default([], true)|length > 0 -%}
inhibit_rules:
  {{ alertmanager_inhibit_rules_config|to_nice_yaml(indent=2)|indent(width=2) }}
{% endif %}
